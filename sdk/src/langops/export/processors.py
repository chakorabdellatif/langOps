"""Span processors.

BatchSpanProcessor configuration for async, non-blocking export, plus the
payload-limit processor that enforces max_payload_bytes on span events.

Implemented in Phase 3 — see tasks.md and architecture.md §5.4.
"""
