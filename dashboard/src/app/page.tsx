// Phase 5 (tasks.md): overview dashboard — recent executions, failure rate,
// cost today, latency sparkline.
export default function OverviewPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-2">
      <h1 className="text-3xl font-semibold">LangOps</h1>
      <p className="text-neutral-400">
        Observability for LangGraph applications — dashboard under construction.
      </p>
    </main>
  );
}
