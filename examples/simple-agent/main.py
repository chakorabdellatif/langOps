"""Minimal instrumented LangGraph app (implemented in Phase 3 — tasks.md).

Target shape: a small two/three-node StateGraph with one LLM call and one
tool call, instrumented with `langops.instrument(graph)`, used both as the
quickstart demo and as the E2E smoke test fixture.
"""

raise SystemExit("Implemented in Phase 3 — see tasks.md")
