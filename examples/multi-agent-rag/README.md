# multi-agent-rag

Richer demo for LangOps: multiple agents, tool use, retrieval, a
checkpointer (thread/checkpoint metadata, resumed runs), and retries.

Built in Phase 6 of `tasks.md` — it is the acceptance fixture for the state
viewer and context-observation features.
